/**
 * Injected by build.mjs via esbuild `define`. The stamp of the page-functions
 * module bundled into this extension, reported in `extension-hello` so the
 * broker can tell a stale in-memory copy from a fix that was never shipped.
 */
declare const PAGE_FUNCTIONS_STAMP: string;

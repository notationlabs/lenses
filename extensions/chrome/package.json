{
  "name": "@djgrant/lenses-extension-chrome",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "check:metadata": "node scripts/validate-metadata.mjs",
    "release:zip": "pnpm build && node scripts/package-release.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@djgrant/lenses-core": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}

{
  "name": "lenses",
  "private": true,
  "version": "0.1.0",
  "description": "Typed webpage functions for TypeScript, CLI, and MCP consumers.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@11.6.0",
  "devDependencies": {
    "@djgrant/lens": "workspace:*",
    "@pokit/core": "^0.6.0",
    "@pokit/terminal": "^0.6.0",
    "pok-plugins": "github:djgrant/pok-plugins",
    "pokit": "^0.3.2"
  }
}
